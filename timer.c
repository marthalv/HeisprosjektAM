#include "timer.h"
#include <stdio.h>
/*
void timer_delay(struct Timer* timer, int delay) {
	timer->time_out = (int)clock() + delay*CLOCKS_PER_SEC;
}

int timer_time_is_up(struct Timer* timer) {
	return ((int)clock() >= timer->time_out);
}






int timer_start_timer() {
    time_t time_now = time(NULL);
    return time_now;
}

int timer_time_is_up(int start_time) {
    if (time(NULL) > (start_time + 3)) {
        return 1;
    }
        return 0;
}
*/
